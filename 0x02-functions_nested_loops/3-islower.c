#include <ctype.h>
#include "main.h"

/**
 * _islower - Check if character is lowercase
 * @c: character to be evaluated
 * Return: returns 1 if character is lowercase, 0 if it isnt
 */
int _islower(int c)
{
	value = islower(c) == 0 ? 0 : 1;
	return (value);
}
