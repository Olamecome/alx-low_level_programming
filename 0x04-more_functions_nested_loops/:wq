#include "main.h"

/**
 * more_numbers - FUnction to print numbers in string except 2 and 4
 * Return: Function returns 0 - Success
 */
void more_numbers(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		for (j = 0; j <= 14; j++)
		{
			if (j > 9)
				_putchar(j / 10 + '0');

			_putchar(j % 10 + '0');
		}
		_putchar('\n');
	}
}
