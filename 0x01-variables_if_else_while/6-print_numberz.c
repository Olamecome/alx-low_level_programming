#include <stdio.h>

/**
 * main - FUnction to print numbers in string
 * Return: Function returns 0 - Success
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
