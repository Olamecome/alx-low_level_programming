#include <stdio.h>

/**
 * main - This is the main function that prints with printf
 * description - This returns a 0 value
 * Return: Zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
